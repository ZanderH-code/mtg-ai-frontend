services:
  - type: web
    name: mtg-ai-frontend
    env: static
    buildCommand: yarn install && yarn build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://mtg-ai-backend.onrender.com
